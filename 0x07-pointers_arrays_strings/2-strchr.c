#include "main.h"
#include <string.h>

/**
 * _strchr - returns frst occurance of letter
 * @s: parameter s
 * @c: parameter
 * Return: string from first occurance of letter
 *
 */
char *_strchr(char *s, char c)
{
	return (strchr(s, c));
}
