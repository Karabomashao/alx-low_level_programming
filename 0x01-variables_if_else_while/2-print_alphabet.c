#include <stdio.h>

/**
 *main - use a putchar function to display a-z alphaberts
 *Return true if 0 else otherwise 
 */
int main(void)
{
	int chr;

	for (chr = "a"; chr <= "z"; chr++)
		putchar(ch);
	return (0);
}
