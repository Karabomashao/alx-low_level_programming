#include <stdio.h>

/**
 * main - print base 10 numerals from 0 - 9
 * Return - True if 0 false otherwise
 */
int main(void)
{
	int n;

	for (n = 0; n <= 9; n++)
	{
		printf("%d\n", n);
	}
	return (0);
}
