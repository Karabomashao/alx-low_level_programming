#include "main.h"
#include "string.h"

/**
 * _strcat - function concatenates two strings
 * @dest: parameter for first string
 * @src: parameter for second string
 * Return: concatenated string
 */
char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
