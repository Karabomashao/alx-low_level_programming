#include "main.h"

/**
 * mul - multipliers two numbers
 * @a: parameter a
 * @b: parameter b
 * Return: mutiplied numbers
 */
int mul(int a, int b)
{
	int multiplier;

	multiplier = a * b;
	return (multiplier);
}
