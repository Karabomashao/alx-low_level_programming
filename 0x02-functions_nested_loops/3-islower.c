#include "main.h"

/**
 * _islower - checks lowercase characters
 * @c: a character argument
 * Return: prints 0 or one depending on the conditiond
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
