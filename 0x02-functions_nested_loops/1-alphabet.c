#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (succesful)
 */
int main(void)
{
	char i;

	for (i = 97; i <= 122; i++)
	{
		putchar(i)
	}
	putchar('\n')
	return: Always 0 (successful)
}
