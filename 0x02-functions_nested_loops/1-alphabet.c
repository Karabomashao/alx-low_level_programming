#include <stdio.h>
#include <main.h>

/**
 * main - Entry point
 * Return: Always 0 (succesful)
 */
int main(void)
{
	char i;

	for (i = 97; i <= 122; i++)
	{
		putchar(i);
	g
	putchar('\n');
	return (0);
}
